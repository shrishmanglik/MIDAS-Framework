{
  "hooks": [
    {
      "type": "PostToolUse",
      "matcher": {
        "tool_name": "write_file",
        "path_pattern": "app/**/*.py"
      },
      "hooks": [
        {
          "type": "command",
          "command": "ruff check --select E,F,I --quiet ${file_path} 2>/dev/null || echo '⚠ Lint issues detected — run ruff check'"
        }
      ]
    }
  ]
}
